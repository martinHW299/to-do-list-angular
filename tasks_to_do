1. Add login component as first interaction with the user.
    * Add user interface
    * Add persistant data of users with pass encrypted
    * Check validation user and password
    * Show message in case of an error in user or pass input  
2. Look for the possibility to add a tracking bar.
3. Add set date and time option for every task.